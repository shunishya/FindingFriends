<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/light_theme_color"
    android:gravity="center" >

    <TextView
        android:id="@+id/info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:text="If you have already registered then use your own password and name, if not then choose your own."
        android:textColor="@color/abs__bright_foreground_disabled_holo_dark"
        android:textSize="20sp" />

    <EditText
        android:id="@+id/etName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/info"
        android:layout_margin="10dp"
        android:background="@drawable/edit_text_bg"
        android:hint="@string/enter_your_name"
        android:imeOptions="actionNext"
        android:inputType="textCapSentences"
        android:padding="10dp" />

    <EditText
        android:id="@+id/etPhoneNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etName"
        android:layout_margin="10dp"
        android:background="@drawable/edit_text_bg"
        android:hint="@string/enter_your_phone_number"
        android:imeOptions="actionNext"
        android:inputType="phone"
        android:padding="10dp" />

    <EditText
        android:id="@+id/etPassword"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etPhoneNumber"
        android:layout_margin="10dp"
        android:background="@drawable/edit_text_bg"
        android:hint="Password"
        android:imeOptions="actionDone"
        android:inputType="textPassword"
        android:padding="10dp" />

    <Button
        android:id="@+id/btnSubmit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etPassword"
        android:layout_centerHorizontal="true"
        android:layout_margin="10dp"
        android:text="@string/submit" />

</RelativeLayout>